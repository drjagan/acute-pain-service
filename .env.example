# Acute Pain Service - Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file to .env in the root directory
# 2. Update all values with your actual configuration
# 3. NEVER commit the .env file to version control
# 
# For automated installation, use the installation wizard at /install/
# The wizard will generate the .env file automatically

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aps_database
DB_USER=root
DB_PASS=
DB_CHARSET=utf8mb4

# ============================================
# APPLICATION SETTINGS (REQUIRED)
# ============================================
APP_ENV=production
APP_NAME=Acute Pain Service
APP_VERSION=1.1.3

# Generate a secure random key (64 characters)
# Command: php -r "echo bin2hex(random_bytes(32));"
APP_KEY=

# ============================================
# SESSION CONFIGURATION
# ============================================
SESSION_LIFETIME=3600
SESSION_NAME=APS_SESSION

# ============================================
# SECURITY SETTINGS
# ============================================
PASSWORD_MIN_LENGTH=8
MAX_LOGIN_ATTEMPTS=5
LOGIN_TIMEOUT=900

# ============================================
# PAGINATION & DISPLAY
# ============================================
PER_PAGE=20

# ============================================
# LOGGING
# ============================================
LOG_ENABLED=true
LOG_LEVEL=INFO

# ============================================
# FILE UPLOAD SETTINGS
# ============================================
# Max upload size in bytes (5MB = 5242880)
MAX_UPLOAD_SIZE=5242880
ALLOWED_EXTENSIONS=jpg,jpeg,png,pdf,doc,docx

# ============================================
# TIMEZONE
# ============================================
# See: https://www.php.net/manual/en/timezones.php
APP_TIMEZONE=Asia/Kolkata

# ============================================
# EMAIL CONFIGURATION (OPTIONAL)
# ============================================
# Enable email notifications
SMTP_ENABLED=false

# SMTP Server Settings
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_ENCRYPTION=tls
SMTP_USERNAME=
SMTP_PASSWORD=

# Email Sender Details
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=Acute Pain Service

# ============================================
# BACKUP CONFIGURATION (OPTIONAL)
# ============================================
BACKUP_PATH=/var/backups/aps
BACKUP_RETENTION_DAYS=30

# ============================================
# ADVANCED SETTINGS (OPTIONAL)
# ============================================
# Enable debug mode (NEVER enable in production)
APP_DEBUG=false

# Custom application URL (if behind reverse proxy)
# APP_URL=https://your-domain.com

# ============================================
# NOTES
# ============================================
# - Required fields MUST be configured for the app to work
# - Optional fields can be configured later as needed
# - Keep this file secure and restrict access (chmod 600)
# - Back up this file in a secure location
# - Change default passwords after installation
